<h4><%= @attraction.name %></h4>
<%= button_to "Go on this ride", rides_path,  params: {'attraction' => @attraction} %>
<% if @user.admin? %>
    <p> <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %> </p>
<% end %>
